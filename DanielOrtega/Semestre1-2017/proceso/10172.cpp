#include <stdio.h>
#include <vector>
#include <queue>
#include <stack>
#include <cstring>
#include <iostream>

using namespace std;

int main() {


/*
A:(stack) van todos los se colocan los paquetes que seran trasladados a por aire a un pais X
B: (queue) cola de paquetes que deben ser llevados a un pais disinto
 de x
*/
	int s,b;

	while(scanf("%d %d ", &s,&b) && (s!=0 && b!=0)){
		cout << s << " " << b << endl;
		int derecha[1];
		int izquierda[1];

		//lee numer


	}

		
	return 0;
}